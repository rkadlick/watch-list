# Development Dockerfile - for hot reload and code editing

FROM node:20-alpine

WORKDIR /app

# Install dependencies (will be cached)
COPY package.json package-lock.json* ./
RUN npm ci

# Install Claude Code CLI globally (only in Docker, not local)
RUN npm install -g @anthropic-ai/claude-code

# Copy source code (will be overridden by volume mount, but needed for initial setup)
COPY . .

# Expose port
EXPOSE 3000

# Set environment to development
ENV NODE_ENV=development
ENV NEXT_TELEMETRY_DISABLED=1

# Run dev server with hot reload
CMD ["npm", "run", "dev"]
